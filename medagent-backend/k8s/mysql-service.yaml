apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: medagent
  labels:
    app.kubernetes.io/name: medagent
    app.kubernetes.io/component: mysql
    app.kubernetes.io/part-of: medagent
spec:
  type: ClusterIP
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
      protocol: TCP
  selector:
    app.kubernetes.io/component: mysql
